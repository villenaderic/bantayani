# When testing with Expo Go on a physical phone or an Android emulator,
# localhost refers to the phone itself, not your development machine.
# Use your machine's LAN IP address instead, for example:
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.42:8000/api
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api
